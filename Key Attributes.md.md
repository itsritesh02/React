# What is the Purpose of Key Attributes in react?

---

The key attribute is used to uniquely identify elements in a React list. It helps React identify which items have been added, removed, or changed, so React can update the UI efficiently